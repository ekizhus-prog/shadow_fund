# 55-second demo

1. Open `http://127.0.0.1:8312` and leave `$100,000` / `Balanced` selected.
2. Create the fund and show the actual stage strip from collection to monitoring.
3. Click a token position to show its source influence and observed-exposure trace.
4. Click Evaluate evidence again. The second recorded bundle changes two source exposures and produces a new paper decision when the policy permits it.
5. Open “Replay evidence” on the journal card. Show the read-only replay result and matching source hash.
6. Keep the `SYNTHETIC` label visible; it is a reproducibility fixture, not a live Nansen claim.

If live provider access is later enabled, record the exact data cutoff, endpoint coverage, request/credit usage, and public-data permissions. Do not replace the synthetic fixture with plausible filler.
